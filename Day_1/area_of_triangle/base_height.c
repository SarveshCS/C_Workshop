#include<stdio.h>

void main(){
    float base, height;
    printf("Enter the base: ");
    scanf("%f", &base);
    printf("Enter the base: ");
    scanf("%f", &height);
    printf("Area: %f", (height*base)/2);
}